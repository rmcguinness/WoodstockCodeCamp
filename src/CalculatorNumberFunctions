export const handleClick = (num, display, setDisplay) => {
  if (num.func === 'handleNumberClick') {
    handleNumberClick(num, display, setDisplay);
  }
  else {
    handleDotClick(num, display, setDisplay);
  }

}

const handleNumberClick = (num, display, setDisplay) => {
  if (display === '0') {
    setDisplay(num.val)
  } else {
    setDisplay(display + num.val)
  }
}

const handleDotClick = (num, display, setDisplay) => {
  if (!display.includes(num.val)) { 
    setDisplay(display + num.val)
  }
}





